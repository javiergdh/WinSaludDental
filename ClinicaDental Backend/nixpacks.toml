[phases.setup]
nixPkgs = ["dotnet-sdk"]

[phases.build]
cmds = ["dotnet restore", "dotnet publish -c Release -o out"]

[start]
cmd = "dotnet \"out/ClinicaDental Backend.dll\" --urls=http://0.0.0.0:$PORT"